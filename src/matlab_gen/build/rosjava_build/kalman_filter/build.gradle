/*
 * Copyright 2015-2018 The MathWorks, Inc.
 */

try {
    project.catkin.tree.generateMessageArtifact(project, 'kalman_filter')
} catch (NullPointerException e) {
    println("Couldn't find kalman_filter on the ROS_PACKAGE_PATH")
}

task bugfixtask << {
    mkdir sourceSets.main.output.classesDir
}
jar.dependsOn(bugfixtask)
